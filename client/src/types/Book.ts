export interface Book {
    title: string;
    author: string;
    genre: string;
    description: string;
}

export interface BookResponse {
    id: string;
    title: string;
    author: string;
    genre: string;
    description: string;
}
