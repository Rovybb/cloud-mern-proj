export interface Book {
    title: string;
    author: string;
    genre: string;
    description: string;
}

export interface BookResponse {
    _id: string;
    title: string;
    author: string;
    genre: string;
    description: string;
}
